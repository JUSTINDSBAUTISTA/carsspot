<div class="car-item" onclick="window.location='<%= car_path(car) %>';">
  <div class="car-details">
    <%= image_tag car.image, alt: car.car_name, class: "car-image" if car.image.present? %>
    <div class="car-info">
      <h2><%= car.car_name %></h2>
      <p><%= car.car_make %></p>
      <p class="car-rating"><span class="star">★</span> <%= car.rating %>/5</p>
      <p class="car-mileage"><%= car.mileage %> km</p>
      <p class="car-price"><%= number_to_currency(car.price_per_day) %>/day</p>
    </div>
  </div>
</div>
